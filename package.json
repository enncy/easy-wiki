{
  "scripts": {
    "deploy": "npx ewiki && git add . && git commit -m 'deploy' && git push origin"
  },
  "devDependencies": {
    "ewiki": "^0.0.46"
  },
  "dependencies": {
    "markdown-it-container": "^4.0.0"
  }
}
